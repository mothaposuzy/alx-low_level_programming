#include "main.h"

/**
 * main - prints the name of the program.
 * Return: Always 0 (Success)
 */

int main(void)
{
	const char = "mynameis"

		for (int i = 0, mynameis[i] != '\0'; i++)
		{
			_putchar(mynameis[i]);
		}
	_putchar('\n');
	return (0);
}
