#include "main.h"

/**
 * mul - multiplies two integer
 * @a: the first number to multiply
 * @b: the second number to multiply with the first number
 * Return: the results of the multiplication of the a and b params
 */

int mul(int a, int b)
{
	return (a * b);
}
