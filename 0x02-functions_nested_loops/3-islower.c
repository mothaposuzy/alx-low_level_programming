#include "main.h"

/**
 * _islower - check if char lowercase
 *
 *@c: is the char to be checked
 *
 * Return: 1 if it lowercase, otherwise 0

 int _is lower(int c)
 {
 	if (c >= 'a' && c <= 'z')
		return (1);
	else
		return (0);

 }
